<?xml version="1.0" encoding="utf-8"?><!--
   Sample data extraction rules file; uncomment and customize as necessary.
   See https://developer.android.com/about/versions/12/backup-restore#xml-changes
   for details.

   API 31+ counterpart to backup_rules.xml (same "everything, no rules active" state).
   cloud-backup = what syncs to Google Drive; device-transfer = what copies phone-to-phone
   during setup. Referenced from AndroidManifest.xml via android:dataExtractionRules.
-->
<data-extraction-rules>
    <cloud-backup>
        <!-- TODO: Use <include> and <exclude> to control what is backed up.
        <include .../>
        <exclude .../>
        -->
    </cloud-backup>
    <!--
    <device-transfer>
        <include .../>
        <exclude .../>
    </device-transfer>
    -->
</data-extraction-rules>